# Ref: https://github.com/tmux/tmux/wiki/Getting-Started#list-of-useful-options

# Turn on mouse support
setw -g mouse on

# Allow xterm titles in terminal window, terminal scrolling with scrollbar
set -g terminal-overrides "xterm*:XT:smcup@:rmcup@"

# Scroll History
set -g history-limit 30000

# Set ability to capture on start and restore on exit window data when running an application
setw -g alternate-screen on

# Lower escape timing from 500ms to 50ms for quicker response to scroll-buffer access.
set -s escape-time 50

# setup | and - for window splitting
unbind %
bind | split-window -h
bind - split-window -v

# colors
set -g default-terminal "tmux-256color"

# title
set -g set-titles on
set -g set-titles-string '#T'

# enable automatic window renaming
set-window-option -g automatic-rename on

# active window title colors
setw -g window-status-current-format "|#I:#W|"

# enable extended keys support for terminals that support it
set -g extended-keys on

# If on, windows are automatically renumbered to close any gaps in the window
# list
set-option -g renumber-windows on
