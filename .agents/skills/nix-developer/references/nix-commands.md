# Nix commands reference

This guide contains a reference to Nix commands.

The full command reference is available at
<https://nix.dev/manual/nix/latest/command-ref/command-ref>.

## `nix flake`

- `nix flake info`: show flake metadata
- `nix flake show`: show the outputs provided by a flake
